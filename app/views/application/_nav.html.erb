<header class="nav">
  <div class="nav__inner">
    <% if authenticated? %>
      <span class="nav__brand">Handshake.</span>
      <%= render "application/nav_profile" %>
      <%= render "application/nav_tabs" %>
    <% end %>
  </div>
</header>
