<% content_for :title, "Adapt CV — Handshake" %>

<div class="home-screen">
  <%= render 'opportunities/top_bar' %>
  <%= render 'opportunities/hero' %>
  <%= render 'opportunities/form', opportunity: @opportunity %>
</div>
