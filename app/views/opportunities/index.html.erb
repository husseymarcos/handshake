<% content_for :title, "Opportunities — Handshake" %>

<div class="page-header row row--between">
  <div>
    <h1 class="display">Opportunities</h1>
    <p class="lede">Track your career opportunities.</p>
  </div>
  <%= link_to "New Opportunity", new_opportunity_path, class: "pill pill--accent" %>
</div>

<% if @opportunities.any? %>
  <ul class="card list-plain stack">
    <% @opportunities.each do |opp| %>
      <li class="history-row">
        <div>
          <%= link_to opp.organization_name, opportunity_path(opp), class: "history-row__title" %>
          <div class="muted"><%= l opp.created_at, format: :long %></div>
        </div>
        <% if opp.pdf.attached? %>
          <%= link_to "Resume", opportunity_download_path(opp), class: "pill" %>
        <% end %>
      </li>
    <% end %>
  </ul>
<% else %>
  <div class="card">
    <p class="lede">No opportunities yet.</p>
    <%= link_to "Create your first", new_opportunity_path, class: "pill pill--accent" %>
  </div>
<% end %>
